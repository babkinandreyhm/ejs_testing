
<%for(var key in _old){
%>
<%= new EJS({url:"rt.ejs"}).render({prefix:'',element:_old[key],key:key}) %>
<% } %>

<%for(var key in _new){
%>

<%= new EJS({url:"rt.ejs"}).render({prefix:'new',element:_new[key],key:key}) %>
<% } %>
